Simple CLI tool to download full Bunkr albums.

HOW TO USE:
Run directly in CLI: input album URL and (optionally) download path.
Run as import: call main, best to supply URL and download, otherwise will call python input
